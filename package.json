{
  "name": "whatsappapi",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:prod": "NODE_ENV=production node dist/index.js",
    "start:pm2": "pm2 start dist/index.js --name whatsapp-api",
    "stop:pm2": "pm2 stop whatsapp-api",
    "restart:pm2": "pm2 restart whatsapp-api",
    "logs:pm2": "pm2 logs whatsapp-api",
    "dev": "ts-node index.ts",
    "prebuild": "rm -rf dist",
    "postbuild": "echo 'Build completed successfully'",
    "deploy": "npm run build && npm run start:prod",
    "health": "curl -f http://localhost:3001/health || exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@whiskeysockets/baileys": "^7.0.0-rc.6",
    "axios": "^1.13.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^20.10.0",
    "pm2": "^5.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
